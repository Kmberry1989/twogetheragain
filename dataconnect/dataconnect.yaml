specVersion: "v1"
serviceId: "twogether-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "twogether-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
